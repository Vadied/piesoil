// IMPORTANT: This file resolves to the same URL path (/backoffice/dashboard) as
// src/app/backoffice/dashboard/page.tsx and will cause a Next.js build conflict.
// It must be deleted before running `next build` or `next dev`.
// The real dashboard page is at src/app/backoffice/dashboard/page.tsx.
export {}
